using Godot;
using System;

public partial class Toolbar : Panel
{
    //[Export] Godot.Collections.Array<UIWindow> Windows;
//
    //Node buttonContainer;
//
    //public override void _EnterTree()
    //{
    //    base._EnterTree();
//
    //    buttonContainer = GetNode<Control>("ButtonContainer");
    //    
    //    // create buttons for opening windows
    //    foreach (UIWindow menu in Windows)
    //    {
    //        Button button = new Button();
//
    //        //button.Text = menu.getName();
    //        button.Connect(Button.SignalName.Pressed, Callable.From(() => openMenu(menu)));
//
    //        buttonContainer.AddChild(button);
    //    }
    //}
//
    //public void openMenu(UIWindow menu) => menu.Open();
    
}
